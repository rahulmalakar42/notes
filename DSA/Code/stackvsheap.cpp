#include<stdio.h>
#include<stdlib.h>

int main(){

    //there are mainly three sections in memory
    // heap
        // - program cannot directly access the heap memory
        // - using pointer we can access
        // - word new or malloc is used to allot memory in heap

    // stack
    //     - once we declare a variable
    //     the memory will be allocated inside stack
    //     - the portion of memory given to the stack is stack frame / activation record of function
    //     - this is called static memory allocation : bytes of memory required was decided at compile time
    
    // code : area where machine code is loaded



    return 0;
}